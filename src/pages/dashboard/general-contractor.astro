---
/**
 * General Contractor Dashboard
 * Redirects to the existing projects page which serves as the GC interface
 */
export const prerender = false;

// Redirect to the projects dashboard
return Astro.redirect('/projects');
---
